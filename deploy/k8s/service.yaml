apiVersion: v1
kind: Service
metadata:
  name: library-card-tracker
  namespace: library-card-tracker
  labels:
    app: library-card-tracker
spec:
  selector:
    app: library-card-tracker
  ports:
    - name: http
      port: 80
      targetPort: http
